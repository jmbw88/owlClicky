import React from "react";

const Header = () => (
    <div className="jumbotron p-3">
    <h1 className="text-center">Meet the teams!</h1>
    <h3 className="text-center">Meet the teams of Overwatch League - Click on as many logos you can without clicking the same one.</h3>
    </div>
)

export default Header;